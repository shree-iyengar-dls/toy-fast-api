apiVersion: v1
kind: Service
metadata:
  name: { { include "toy-fast-api.fullname" . } }
  labels: { { - include "toy-fast-api.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "toy-fast-api.selectorLabels" . | nindent 4 } }
