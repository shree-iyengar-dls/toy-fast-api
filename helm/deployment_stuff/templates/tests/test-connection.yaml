apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deployment_stuff.fullname" . }}-test-connection"
  labels:
    {{- include "deployment_stuff.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deployment_stuff.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
