apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployment_stuff.fullname" . }}
  labels:
    {{- include "deployment_stuff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deployment_stuff.selectorLabels" . | nindent 4 }}
